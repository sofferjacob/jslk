#ifndef _hal_h
#define _hal_h

// Since I will only be supporting x86 I will just do the following
#include "../hal/hal.h"

/* But just in case someone chooses to port it to other machines you could maybe do it as follows:
#ifdef x86
    #include "../hal/x86/hal.h"
#elif defined arm
    #include "../hal/arm/hal.h"
#endif
*/

#endif